<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MasterDetails" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryVariant">@color/black</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Define attr color. -->
        <item name="textColor">@color/white</item>
        <item name="themedBackgroundColor">@color/dark_gray</item>
        <item name="toolbarBackgroundColor">@color/dark_gray</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimary</item>
        <item name="toolbarStyle">@color/black</item>
        <!-- Customize your theme here. -->
        <!-- Text input fields -->
        <item name="textInputStyle">@style/Widget.MasterDetails.FilledBox</item>
        <!-- Text view -->
        <item name="android:textViewStyle">@style/Widget.MasterDetails.TextView</item>
        <!-- Buttons -->
        <item name="buttonStyle">@style/Widget.MasterDetails.Button</item>
        <item name="android:buttonStyle">@style/Widget.MasterDetails.Button</item>
        <!--Action bar style-->
        <item name="android:actionBarStyle">@style/AppTheme.ActionBar</item>
        <item name="actionBarStyle">@style/AppTheme.ActionBar</item>
    </style>

    <style name="Theme.MasterDetails.NoActionBar" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryVariant">@color/black</item>
        <item name="colorOnPrimary">@color/white</item>

        <item name="android:statusBarColor" tools:targetApi="l">@color/dark_gray</item>
        <!-- Define attr color. -->
        <item name="textColor">@color/white</item>
        <item name="themedBackgroundColor">@color/dark_gray</item>
        <item name="toolbarBackgroundColor">@color/dark_gray</item>
    </style>
</resources>